'use babel';

import GithubSourceCodeView from '../lib/github-source-code-view';

describe('GithubSourceCodeView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
